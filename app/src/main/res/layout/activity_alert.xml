<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="20dp"
    android:gravity="center"
    android:background="@color/mirage"
    tools:context=".AlertActivity">

  <LinearLayout
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:orientation="horizontal">

    <TextView
        android:id="@+id/txtAlertTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="12dp"
        android:text="Alert Authorities"
        android:textAlignment="center"
        android:textColor="@color/pesto"
        android:textSize="20sp"
        android:textStyle="bold" />

  </LinearLayout>


  <TextView
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:textColor="@color/pesto"
      android:textSize="20sp"
      android:text="Location:"
      android:textStyle="normal"
      android:textAlignment="center"/>
  <EditText
      android:id="@+id/editAlertLocationTextMultiLine"
      android:layout_width="match_parent"
      android:layout_height="40dp"
      android:background="@drawable/rounded_edittext"
      android:backgroundTint="@color/pesto"
      android:ems="10"
      android:gravity="center"
      android:hint="Enter location of incident here"
      android:inputType="textMultiLine"
      android:textColorHint="@color/mirage"
      android:textSize="20sp"
      android:textStyle="bold" />

  <TextView
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:textColor="@color/pesto"
      android:textSize="20sp"
      android:text="Information:"
      android:textStyle="normal"
      android:textAlignment="center"
      android:layout_marginTop="20dp"/>

  <EditText
      android:id="@+id/editAlertTextMultiLine"
      android:layout_width="match_parent"
      android:layout_height="150dp"
      android:background="@drawable/rounded_edittext"
      android:backgroundTint="@color/pesto"
      android:ems="10"
      android:gravity="center"
      android:hint="Enter Details of Tip-off Here"
      android:inputType="textMultiLine"
      android:textColorHint="@color/mirage"
      android:textSize="20sp"
      android:textStyle="bold" />

  <TextView
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:textColor="@color/pesto"
      android:textSize="20sp"
      android:text="Identification:"
      android:textStyle="normal"
      android:textAlignment="center"
      android:layout_marginTop="20dp"/>

  <RadioGroup
      android:id="@+id/radioGroupA"
      android:layout_width="match_parent"
      android:layout_height="96dp">

    <RadioButton
        android:id="@+id/radioButton_Anonymous"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:height="48dp"
        android:background="@color/pesto"
        android:buttonTint="@color/mirage"
        android:checked="true"
        android:minHeight="48dp"
        android:text="ANONYMOUS"
        android:textColor="@color/mirage"
        android:textStyle="bold" />

    <RadioButton
        android:id="@+id/radioButton_NonAnonymous"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:height="48dp"
        android:background="@color/pesto"
        android:buttonTint="@color/mirage"
        android:minHeight="48dp"
        android:text="NON-ANONYMOUS"
        android:textColor="@color/mirage"
        android:textStyle="bold" />

  </RadioGroup>

  <Button
      android:id="@+id/btnAlert"
      android:layout_width="match_parent"
      android:layout_height="48dp"
      android:layout_marginTop="30dp"
      android:layout_marginBottom="20dp"
      android:layout_marginLeft="20dp"
      android:layout_marginRight="20dp"
      android:background="@drawable/roundedbutton"
      android:backgroundTint="@color/alt_btn_background_color"
      android:text="SUBMIT"
      android:textColor="@color/alt_btn_text_color"
      android:textSize="25sp"/>
  <Button
      android:id="@+id/btnAlertHistory"
      android:visibility="gone"
      android:enabled="false"
      android:layout_width="match_parent"
      android:layout_height="48dp"
      android:layout_marginLeft="20dp"
      android:layout_marginRight="20dp"
      android:background="@drawable/roundedbutton"
      android:backgroundTint="@color/alt_btn_background_color"
      android:text="my Alert History"
      android:textColor="@color/alt_btn_text_color"
      android:textSize="25sp"/>

  <Button
      android:id="@+id/btnAlertBackMenu"
      android:layout_width="match_parent"
      android:layout_height="48dp"
      android:layout_marginLeft="20dp"
      android:layout_marginRight="20dp"
      android:layout_marginTop="20dp"
      android:background="@drawable/roundedbutton"
      android:backgroundTint="@color/alt_btn_background_color"
      android:text="Back to Menu"
      android:textColor="@color/alt_btn_text_color"
      android:textSize="25sp"/>



</LinearLayout>