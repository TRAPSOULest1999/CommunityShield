<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:paddingLeft="15dp"
    android:paddingRight="15dp"
    android:background="@color/mirage"
    tools:context=".ForgotPasswordActivity">

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/profile_image"
        android:layout_width="123dp"
        android:layout_height="117dp"
        android:src="@drawable/logo"
        app:civ_border_color="@color/pesto"
        app:civ_border_width="2dp"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="30dp"
        android:orientation="horizontal">


        <TextView
            android:id="@+id/tvForgotPassword"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ems="10"
            android:textAlignment="center"
            android:textSize="20sp"
            android:inputType="text"
            android:text="Forgot Password"
            android:backgroundTint="@color/pesto"
            android:textColorHint="@color/pesto"
            android:textColor="@color/sycamore"/>
    </LinearLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="30dp"
        android:orientation="horizontal">


        <TextView
            android:id="@+id/tvForgotPasswordHelptxt"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ems="10"
            android:textAlignment="center"
            android:textSize="15sp"
            android:inputType="textMultiLine"
            android:text="Please enter email address below and we will send an email with a link to resest password, enter valid email address. If email is not in the found, no link will be sent."
            android:backgroundTint="@color/pesto"
            android:textColorHint="@color/pesto"
            android:textColor="@color/pesto"/>
    </LinearLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="30dp"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/image_fp_email"
            android:layout_width="58dp"
            android:layout_height="40dp"
            app:srcCompat="@drawable/ic_email" />

        <EditText
            android:id="@+id/etFPEmailAddress"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:ems="10"
            android:inputType="textEmailAddress"
            android:hint="Email"
            android:backgroundTint="@color/pesto"
            android:textColorHint="@color/pesto"
            android:textColor="@color/pesto"/>
    </LinearLayout>

    <Button
        android:id="@+id/btnForgotPassword"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_marginTop="30dp"
        android:layout_marginLeft="20dp"
        android:layout_marginRight="20dp"
        android:background="@drawable/roundedbutton"
        android:backgroundTint="@color/alt_btn_background_color"
        android:text="Submit"
        android:textColor="@color/alt_btn_text_color"/>



</LinearLayout>